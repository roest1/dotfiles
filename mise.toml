# @generated by tools/gen-mise.sh from deps.conf — do not edit the tool list.
#
# Add or remove tools in deps.conf, then run: make mise-lock
# Versions here are yours to change; regenerating preserves them.
# Checksums and per-platform URLs live in mise.lock.

[tools]
bun = "1.3.14"
fzf = "0.74.2"
lua-language-server = "3.19.0"
neovim = "0.12.4"
qsv = "22.0.1"
shellcheck = "0.11.0"
stylua = "2.5.2"
tree-sitter = "0.26.12"
